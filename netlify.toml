[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  # Disable secrets scanning for documentation and server files
  SECRETS_SCAN_OMIT_PATHS = "*.md,server/**,database/**"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
